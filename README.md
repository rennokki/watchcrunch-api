# WatchCrunch ⏱️

## Installation

Copy-paste the following command:

```bash
composer install ; \
cp .env.example .env ; \
php artisan key:generate
```

## Tests

Run tests with PHPUnit:

```bash
./vendor/bin/phpunit
```
